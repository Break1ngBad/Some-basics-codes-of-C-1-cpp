#include <stdio.h>

int main()
{
	float halfValue = 0.4 + 0.1;
	float piValue = 3+0.141593;
	printf("The value of half is: %f\n", halfValue);
	printf("The value of Pi is: %f\n", piValue);
	return 0;
}